<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>상품 상세 페이지</h1>
    <p>아이디 : <%= product.id %></p>
    <p>제품명 : <%= product.name %></p>
    <p>제품단가 : <%= product.money %></p>
    <p>제품종류 : <%= product.type %></p>
    <img src="<%= product.image %>" width="100" height="100" />
    <form action="/admin/product/update/<%= product.id %>" method="get">
      <button>수정</button>
    </form>
    <form
      action="/admin/product/<%= product.id %>?_method=DELETE"
      method="post"
    >
      <button>삭제</button>
    </form>
  </body>
</html>
